# Provedor de LLM compatível com a API da OpenAI (chat completions + embeddings)
OPENAI_API_KEY=sua-chave-aqui
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Limites do RAG (controle de custo — capítulo 24)
RAG_TOP_K=4
RAG_MAX_TOKENS=600
