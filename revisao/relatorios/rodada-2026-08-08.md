# Relatório de Revisão — Rodada 1

**Data:** 2026-08-08
**Escopo:** infraestrutura LaTeX, capítulos 01–04 (texto completo), esqueletos
05–25, apêndices A–F, repositório de código (projetos 01–04).
**Resultado geral:** aprovado com ressalvas (itens de acompanhamento abaixo).

## Validações mecânicas (scripts)

| Check | Resultado |
|-------|-----------|
| `scripts/verificar-latex.sh` — includes existem | ✅ 0 erros |
| idem — arquivos de código referenciados | ✅ 0 erros |
| idem — `\label`/`\ref` cruzados | ✅ 0 erros |
| idem — balanceamento de ambientes/chaves | ✅ 0 erros |
| `node --check` — cap01 e cap04 JS | ✅ sintaxe válida |
| Execução manual dos projetos 01–04 | ✅ arquivos íntegros (HTML/CSS/JS) |

## Revisão técnica (achados)

| # | Capítulo | Descrição | Severidade | Correção |
|---|----------|-----------|------------|----------|
| 1 | Cap. 4 | Emojis das cartas podem não renderizar no PDF conforme a fonte mono instalada | menor | Nota tipográfica adicionada à caixa Dica (feito) |
| 2 | Cap. 15 | Chaves dentro de `\texttt{<form action={...}>}` não imprimiam | menor | Escapadas com `\{\ldots\}` (feito) |
| 3 | Global | Validação final exige LaTeX real (compilação) | acompanhamento | Rodar `build-pdf.sh` após instalar TeX (pendente de ambiente) |

## Revisão pedagógica (destaques)

- Parte I tem progressão linear correta (mercado → HTML → CSS → JS) com um
  projeto por capítulo e 3 níveis de exercício;
- Esqueletos 05–25 mantêm o mesmo padrão editorial (objetivos verificáveis,
  critérios de aceite mensuráveis, referências oficiais);
- Acompanhamento: expandir o texto corrido dos capítulos 05–25 (rodada 2).

## Revisão de fontes (achados)

| # | Tema | Descrição | Severidade | Correção |
|---|------|-----------|------------|----------|
| 1 | Mercado | Percentuais do SO Survey 2025 citados como "aproximadamente" | menor | Mantido por honestidade estatística; fonte oficial citada |
| 2 | Versões | Confirmar Next.js 16.3/React 19.2/TS 6/PG 18.4 em reconsulta trimestral | acompanhamento | Agenda em `MANUTENCAO.md` |

## Rodada 1.1 — Compilação real (TinyTeX local, 08/08/2026)

| Check | Resultado |
|-------|-----------|
| `build-pdf.sh` (XeLaTeX + biber) | ✅ exit 0 — `main.pdf`, 183 páginas |
| Overfull hbox (estouro de borda) | ✅ 0 |
| Underfull hbox | ✅ 0 |
| Referências indefinidas (`\ref`/`??`) | ✅ 0 |
| `build-epub.sh` (tex4ebook) | ✅ conversão OK; empacotado via `empacotar-epub.py` (sem `zip` no Windows) |
| EPUB validado | ✅ `mimetype` primeiro, sem compressão; `META-INF/container.xml` presente; 48 arquivos |

Correções aplicadas durante a compilação:
- Fontes passaram a ser carregadas **por nome de arquivo via kpathsea**
  (o fontconfig do Windows não enxergava TeX Gyre; fallback Latin Modern sem
  `lmodern` no mirror) — `preambulo.tex`;
- `\shorthandoff{"}` no preâmbulo (o babel português ativava `"` e quebrava
  aspas em `\texttt{...}`);
- `\texttt{#id}` → `\texttt{\#id}` (cap. 3) e `\texttt{target="\_blank"}` (cap. 2);
- Comandos de autoria (`\autordolivro` etc.) movidos para o preâmbulo
  (eram usados na capa antes de serem definidos);
- Capa com versão condicional para EPUB (`\ifdefined\HCode`), evitando o
  tikz→SVG que falhava no tex4ht.

## Rodada 2 — Expansão editorial completa (08/08/2026)

**Escopo:** expandir os capítulos 05–25 de esqueleto para texto completo no
padrão didático da Parte I, recompilando o PDF após cada capítulo.

| Check | Resultado |
|-------|-----------|
| Capítulos 05–25 expandidos | ✅ 21 capítulos (Git, TS, React, Next, estilização, Node, HTTP/REST, PostgreSQL, ORMs, autenticação, full stack, testes, Docker, CI/CD, segurança, performance, arquitetura, observabilidade, cloud, IA, carreira) |
| Compilação após cada capítulo | ✅ exit 0 em todos |
| Overfull hbox (estouro de borda) | ✅ **0** no build final |
| Referências indefinidas | ✅ 0 |
| PDF final | ✅ `main.pdf`, **233 páginas** (de 183 na rodada 1) |
| EPUB regenerado com texto completo | ✅ `main.epub` (305 KB, spec-compliant) |
| `scripts/verificar-latex.sh` | ✅ 0 erros / 0 avisos |

Correções de tipografia aplicadas durante a rodada 2:
- 8 parágrafos com `\texttt` longo (caminhos, URLs, tipos TS) causavam
  overfull de até 93 pt — corrigidos com `\path{}` (quebrável), `\url{...}`,
  encurtamento de frase e pontos de quebra;
- Tabela do cap. 1 (mercado): colunas `X` com pesos proporcionais + `\raggedright`
  (eliminou overfull e underfulls de badness 10000);
- Tabela OWASP (cap. 19): coluna de código alargada para 1,6 cm;
- Tabela de comparação arquitetura (cap. 21) com colunas `X` proporcionais;
- Ficha técnica (front matter): coluna de rótulo alargada;
- Título de seção e item do cap. 20 encurtados;
- `build-epub.sh`: o tex4ebook falha no Windows só na etapa do `zip` — o
  script agora não aborta e empacota via `empacotar-epub.py` sempre que o
  `.epub` não for criado;
- Apêndice E atualizado (histórico v0.2 e mudanças futuras refletem o texto
  completo);
- Capítulo 24: removida referência cruzada para frente (cap. 25).

### Plano de correção (próxima rodada)

- [x] Expandir capítulos 05–25;
- [ ] Implementar os projetos de código 05–25 no `codigo/` (SkillHub etc.);
- [ ] Reconsultar versões (nov/2026);
- [ ] Validar URLs de `referencias.bib` com verificação automatizada;
- [ ] Instalar `tidy` (opcional) para EPUB ainda mais rigoroso.
